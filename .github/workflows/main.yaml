name: Node.js CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Installing backend packages
      run: npm i
    - name: Installing frontend packages and building
      run: |
        cd frontend
        npm i
        npm run build
